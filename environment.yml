name: goes-viirs
channels:
  - conda-forge
dependencies:
  - python=3.11
  # --- reading the NetCDF / HDF5 files ------------------------------------
  - netcdf4>=1.7
  - h5py>=3.12
  - h5netcdf>=1.3
  - numpy>=1.26
  - pyyaml>=6.0
  - scipy>=1.11
  - pillow>=11
  # --- maps and figures ----------------------------------------------------
  - matplotlib>=3.10,<4
  - cartopy>=0.25,<0.26       # Natural Earth coastlines
  - shapely>=2.0
  # --- notebooks -----------------------------------------------------------
  - jupyterlab>=4.4,<5
  - ipykernel>=6.29,<7
